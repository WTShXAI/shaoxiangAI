# 哨响AI 铁律 — 已清除（2026-08-23）

本文件原载哨响AI全部建模铁律（v2.1）。按用户 2026-08-23 指令「新模型不需要考虑哨响的规则 / 把所有铁律清除」，本文件内容已清空。

后续建模（含 SXAI v2 教练回路）**不再受下列旧约束限制**：
- 禁用原始赔率作特征 / 抗诱导特征铁律
- 单庄无 edge 预设
- 盘口锚定操盘手 100% 跟盘
- 价值层 / +EV 信号约束（用户明确不需要 EV）
- 初盘优先于收盘等数据口径铁律
- CS波胆=庄家资金引导器 类诱导定性铁律

**保留项（通用工程纪律，非哨响专属铁律，继续沿用）**：
- 数据有据可查、未知填 `--` 不填 0
- 不编造赛果、不覆盖用户实时观测
- 命中率并排 naive 基线、显著性检验（二项/McNemar）
- 日志 ASCII、SQLite 单引号、停服务整树 taskkill 等崩溃防护
