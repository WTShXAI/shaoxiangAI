# =============================================================================
# 哨响AI (ShXAI) - 项目依赖 (根目录锁定版本)
# =============================================================================
# 合并自: 根目录 requirements.txt + backend/requirements.txt
# 最后更新: 2026-06-28
# =============================================================================

# ── API / Web ──
fastapi==0.138.0
uvicorn[standard]==0.49.0
a2wsgi==1.10.10
httpx==0.28.1
requests==2.32.3
python-multipart==0.0.32

# ── 数据库 ──
SQLAlchemy==2.0.51
alembic>=1.18.0
redis>=5.0.0,<8.0.0

# ── 异步任务 ──
celery[redis]>=5.6.0

# ── 认证 ──
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ── 数据科学 ──
numpy==2.5.0
pandas==3.0.3
scikit-learn==1.9.0
xgboost==3.3.0
lightgbm==4.6.0
joblib==1.5.3
scipy==1.18.0
sympy==1.14.0

# ── 深度学习 ──
torch==2.12.1
transformers==5.12.1
tokenizers==0.22.2
sentencepiece==0.2.1
accelerate==1.14.0
peft==0.19.1
safetensors==0.8.0
bitsandbytes==0.49.2

# ── 监控 ──
prometheus-client>=0.20.0
psutil==7.2.2

# ── MLflow ──
mlflow>=2.14.0

# ── LLM / LangChain ──
langchain>=1.3.7
langchain-core>=1.4.4
langsmith>=0.8.14
openai>=2.41.0

# ── 数据采集 / 序列化 ──
fastparquet==2026.5.0
cramjam==2.11.0
PyYAML==6.0.3
tomlkit>=0.15.0

# ── 工具 ──
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
tenacity>=9.0
typer==0.25.1
tqdm==4.68.3
rich==15.0.0
pygments==2.20.0

# ── 基础设施 ──
starlette==1.3.1
anyio==4.14.0
click==8.4.1
h11==0.16.0
httpcore==1.0.9
idna==3.18
certifi==2026.6.17
Jinja2==3.1.6
MarkupSafe==3.0.3
greenlet==3.5.2
colorama==0.4.6

# ── 测试 ──
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
