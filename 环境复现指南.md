# 哨响AI 环境复现指南（P3-1）

> 目的：让一名**无口头指导**的工程师从零把系统跑起来（代码→环境→数据→服务→验证）。
> 适用范围：Windows 10/11 单机私有部署（生产仓 `D:\Architecture`）。
> 配套：铁律见 `docs/IRON_RULES.md`；架构/数据流见 `docs/system_design.md`、`docs/class-diagram.mermaid`。

---

## 1. 系统与资源前提

| 项 | 要求 |
|---|---|
| OS | Windows 10/11 64-bit |
| 磁盘 | 系统盘 + **数据盘 ≥ 150GB 可用**（events.db 35GB + GQ.db 6.2GB + 备份；前端/模型较小） |
| 内存 | ≥ 16GB（events.db 查询、LightGBM 训练） |
| 网络 | 可访问 GitHub（SSH 22 或 HTTPS 443）；乐鱼(GQ)采集需有效 token |
| 权限 | 管理员（建计划任务、绑端口 9000） |

---

## 2. 代码获取

```bash
# ⚠️ 本机 DNS 返 IPv6 但路由不通, 先钉死 github→IPv4(全局一次)
git config --global http.curloptResolve "github.com:443:20.205.243.166"

git clone git@github.com:WTShXAI/shaoxiangAI.git D:\Architecture
cd D:\Architecture
git branch --show-current      # 应是 main
```

- 仓库：`WTShXAI/shaoxiangAI`，主分支 `main`。
- 若 SSH 22 不通：改用 HTTPS `https://github.com/WTShXAI/shaoxiangAI.git`（同样受 IPv6 坑影响，已用 curloptResolve 缓解）。

---

## 3. Python 后端环境（生产/训练统一）

> **铁律(IR-09 / 运行环境铁律)**：一律用 `D:\Architecture\.venv\Scripts\python.exe`（Python 3.12.10 + scikit-learn 1.6.1）。裸 python 3.13 / 系统 3.12 非项目环境，会触发 `InconsistentVersionWarning` 且行为不一致。

```bash
cd D:\Architecture
python -m venv .venv                 # 若 .venv 不存在
.venv\Scripts\activate
pip install -r requirements.txt      # 见第 4 节依赖清单
```

验证：
```bash
.venv\Scripts\python.exe --version   # Python 3.12.10
.venv\Scripts\python.exe -c "import sklearn,lightgbm,joblib,pandas,numpy; print('ok')"
```

**所有脚本/训练/回测统一前缀**：
```bash
D:\Architecture\.venv\Scripts\python.exe scripts/<xxx>.py
# 或先激活: .venv\Scripts\activate 再 python scripts/<xxx>.py
```

### 4. 依赖清单（requirements.txt 节选，务必锁定区间）
- API/Web：`fastapi<0.140`, `uvicorn[standard]<0.40`, `httpx`, `requests`, `python-multipart`
- DB：`SQLAlchemy<2.1`, `redis<6`
- 数据科学（**核心预测必需**）：`numpy<2.1`, `pandas<3`, `scikit-learn<1.7`, `xgboost<3`, `lightgbm<5`, `joblib<2`, `scipy<2`
- 深度学习 CPU 版：`torch<3`（反序列化 `football_v4.1_production.joblib` 必需）
- 认证：`python-jose[cryptography]`, `passlib[bcrypt]`
- 监控：`prometheus-client`, `psutil`
- 工具：`pydantic<3`, `pydantic-settings`, `PyYAML`, `python-dotenv`, `typer`, `tqdm`, `rich`, `Jinja2`
- 桌面 GUI：`PySide6<6.12`（app.py 桌面终端必需）
- 前端另需 **Node ≥ 22**（见第 6 节）

---

## 5. 数据层（`data/`）

> 关键真相源：`events.db`（生产赔率时序 + 赛果，35GB）+ `GQ.db`（2026 同期赛果，6.2GB）是 P0 干净验证宇宙的基础。`football_data.db`（历史训练/参考）。其余为派生/缓存。

| 文件 | 体积(约) | 角色 | 复现方式 |
|---|---|---|---|
| `events.db` | 35 GB | **生产主库**：odds_snapshots(1.04亿)/odds_changes(3585万)/matches/prediction_ledger | 采集器持续写入；新机从备份还原或重新采集 |
| `GQ.db` | 6.2 GB | 2026 同期赛果(matches 1.1万/outcomes)，P0 真相源 | 同 events，或随采集累积 |
| `football_data.db` | 724 MB | 历史赛果/赔率/william_ht，模型训练参考 | 随仓库或备份 |
| `indep_features_gq.db` | 1.8 MB | P0-9b/10 的 2026 独立特征（脚本生成） | `python scripts/build_gq_indep_features.py` 重建 |
| `shaoxiang_feature_library.db` | 2.5 MB | 特征库 | 随仓库 |
| `rollball_training.db` | 62 MB | 滚球训练 | 随仓库 |
| `ah_opening_analysis.db` / `ou_opening_analysis.db` | 4MB / 7MB | 盘口开线分析 | 随仓库/脚本 |
| 其他(`leisu_odds`/`long_images`/`worldcup_screenshots`/`bets`/`sim_bets`/`quant_trading`...) | 小 | 缓存/辅助 | 非核心，缺失可后补 |

**数据质量地雷（接手必读，否则结论全错）**：
- `events.db.matches` 赛果被**污染**：平局率膨胀 35-42%（真实应 ~25%），含占位垃圾场。其 `score_*` 仅作"未过滤"参考，**不得**直接当 OOS 考卷。
- `football_data.db` 内 62% 完场比分为假 0-0（冷门联赛未抓比分却写 0-0）。回测须先过 `clean_outcomes`（`score_missing=1`）。
- **干净真相宇宙** = `GQ.db.matches`(平局率 26.7% 真实) ∩ `events.odds_snapshots` 1X2（按 `match_key` 关联）→ 2540 场。任何能力验证都走这条，勿用 events.matches 当赛果。
- `GQ.db.odds_snapshots` 是 2026-08-27 前的**历史表**（OU 线嵌 market 名），**非滚球数据源**；滚球实时盘口源 = `events.db.odds_snapshots`。

---

## 6. 前端环境

```bash
cd D:\Architecture\frontend
node --version     # 需 v22.x（当前 22.22.2）
npm install
npm run dev        # 开发服 :3000（仅本地预览）
# 生产构建: npm run build  -> 由 bridge 伺服 dist/
```

- 前端为 Vite + React + TS；类型严格（tsconfig noUnused）、eslint8。
- 接入后端时 `sport_key` 必须传**空串**（默认 wc 会触发 goal_scale 1.35 虚高 λ）。

---

## 7. 服务启动（Task Scheduler 常驻，非手动）

> **致命坑**：在本机 Bash/PowerShell 会话里 `python xxx.py` 拉起的子进程，**命令结束即被会话回收杀掉**。唯一能常驻的方式是 **Windows 计划任务**（DETACHED_PROCESS）。

### 7.1 计划任务清单（已建，名称固定）
| 任务名 | 保活对象 | 备注 |
|---|---|---|
| `ShaoxiangBridge_Watchdog` | bridge_service.py :9000 | 每 5 分钟兜底重建 |
| `ShaoxiangGQ_Watchdog` | ws_collector（乐鱼采集） | 当前采集器 |
| `ShaoxiangAI_NetWatch` | network_watchdog | 网络自愈 |
| `ShaoxiangVite` | 前端 dev server | 动作=`D:\Architecture\start_vite.bat` |
| `ShaoxiangAI_DailyRecheck` | 每日复评自动化 | 06:07 跑 P0 纸盘等 |
| `ShaoXiangOddsAssetDaily` | 数据资产日报 | — |
| `ShaoxiangSandbox2301` | 沙箱 | 一般停用 |

### 7.2 启用 / 触发 / 停用
```powershell
# 启用并立即触发（恢复服务）
schtasks /change /tn "ShaoxiangBridge_Watchdog" /enable
schtasks /run   /tn "ShaoxiangBridge_Watchdog"
# 同样处理 GQ_Watchdog / NetWatch / Vite
# 停用（先禁用任务再杀进程, 否则看门狗 1~5 分钟内拉回）
schtasks /change /tn "ShaoxiangBridge_Watchdog" /disable
```

### 7.3 手动兜底（仅调试，非长期）
```bash
# bridge（生产 API :9000）
.venv\Scripts\python.exe bridge_service.py --port 9000
# 乐鱼采集（DETACHED 拉起, 勿用 auto_collector 双写!）
pythonw.exe gq\start_collector.py          # 内部起 ws_collector.py -d 0
# 网络看门狗
.venv\Scripts\python.exe scripts\network_watchdog.py
```
> ⚠️ **`auto_collector.py` 故意不单独跑**：会与 ws_collector 双写 odds_snapshots 破坏数据（见 `gq/auto_collector.py:10,29`）。

### 7.4 乐鱼 token
- token 在 `gq/.env`（`GQ_REQUEST_ID` / `GQ_H5_URL`），CUID 常量 `526002076777845380`。
- **token 为密钥，从安全渠道获取，勿入库/勿明文外传**。换号后用 `gq/start_collector.py` 重启生效（健康时 .env 不重读）。

---

## 8. 关键环境坑速查

| 坑 | 现象 | 解决 |
|---|---|---|
| **IPv6 卡死** | git/curl 连 github 21s 超时（DNS 返 IPv6 不通） | `git config --global http.curloptResolve "github.com:443:20.205.243.166"`；或 `curl -4` |
| **会话子进程被杀** | 手动起的 python/node 几分钟内消失 | 用计划任务常驻（§7），勿依赖 Bash 会话 |
| **`rm` 不释放空间** | `rm` 大文件后 `df` 不变 | 本机 `rm` 移入 `$Recycle.Bin`；清空间须删 `D:\$Recycle.Bin` 对应 `$R`+`$I` 项 |
| **假0-0 污染** | 回测 OU 偏移 −4.34pp | 一律先 `import clean_outcomes` 过滤 `score_missing=1` |
| **events.matches 平局膨胀** | P0 用其当考卷→假不及格 | 真相走 `GQ.db.matches` ∩ `events.odds_snapshots`（2540 场） |
| **sport_key 非空** | 前端 λ 虚高 12% | 传空串 |
| **用错 python** | InconsistentVersionWarning | 一律 `.venv\Scripts\python.exe` |
| **auto_collector 双写** | odds_snapshots 重复 | 只跑 ws_collector |

---

## 9. 验证（GO 状态自检）

```bash
# 1) bridge 健康
curl -s http://localhost:9000/health      # 期望 healthy, events_db_snapshot_age_min 接近 0
# 2) 采集新鲜
#    events.db.odds_snapshots 最近 captured_at 距当前 < 10s
# 3) 环境冒烟
.venv\Scripts\python.exe -c "import pipeline.william_inter_model as m; print(m.predict_1x2(2.1,3.3,3.1,1.9,3.4,3.6))"
# 4) P0 干净真相可复现（可选）
.venv\Scripts\python.exe scripts\p0_10_retrain_independent_gq.py   # 应复现三折 +EV 强烈为负(P0=FAILED)
```

GO 判据：`/health`=200 且 snapshot 新鲜 + 上述冒烟通过。

---

## 10. 常用命令速查

```bash
# 跑任意脚本（铁律前缀）
D:\Architecture\.venv\Scripts\python.exe scripts/<name>.py

# 重建 2026 独立特征（P0-9b/10 输入）
.venv\Scripts\python.exe scripts\build_gq_indep_features.py

# 重启全部服务（先禁用对应任务再杀进程, 再启用触发）
# 见 §7.2

# 估值/能力验证报告
#   reports/p0_10_retrain_status.md  (P0 干净真相 FAILED 结论)
#   deliverables/哨响AI_系统估值报告_P2里程碑_20260916.html
```

---

## 11. 交接检查清单（P3-5 演练用）
- [ ] 新机 git clone + curloptResolve 生效
- [ ] `.venv` 建好且 sklearn/lightgbm/torch 可 import
- [ ] `data/` 核心库（events/GQ/football_data）就位
- [ ] 7 个计划任务状态正确（bridge/gq/net/vite 启用，sandbox 停用）
- [ ] `/health` 200 + 快照新鲜
- [ ] 能无口头指导跑通一个 P0 脚本并复现结论
- [ ] 已知数据地雷（假0-0 / events 平局膨胀）已被接手人理解
