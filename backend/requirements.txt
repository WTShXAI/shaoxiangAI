# =============================================================================
# footballAI Backend - FastAPI + SQLAlchemy + Celery
# 最后更新: 2026-06-28 (合并根目录 requirements.txt 版本约束)
# =============================================================================

# ── Web / API ──
fastapi>=0.136.0
uvicorn[standard]>=0.49.0
a2wsgi>=1.10.0
httpx>=0.27.0
requests>=2.31.0

# ── 数据库 ──
sqlalchemy>=2.0.0
alembic>=1.18.0
redis>=5.0.0,<8.0.0

# ── 异步任务 ──
celery[redis]>=5.6.0

# ── 认证 ──
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ── 数据科学 ──
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
xgboost>=2.0.0
joblib>=1.3.0
scipy>=1.11.0
sympy>=1.13.1

# ── NLP / 深度学习 ──
tokenizers>=0.21.0
sentencepiece>=0.2.0

# ── 监控 ──
prometheus-client>=0.20.0
psutil>=5.9.0

# ── MLflow ──
mlflow>=2.14.0

# ── LLM / LangChain ──
langchain>=1.3.7
langchain-core>=1.4.4
langsmith>=0.8.14
openai>=2.41.0

# ── 工具 ──
pydantic>=2.0.0
pydantic-settings>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
tenacity>=9.0
tomlkit>=0.15.0
typer>=0.26.0

# ── 测试 ──
pytest>=8.0.0
pytest-asyncio>=0.24.0
