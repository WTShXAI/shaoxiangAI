# 哨响AI 关键决策记录（P3-3）

> 目的：把每条铁律（IR-01~IR-31）的**「为什么」**沉淀下来，让接手人无需口头指导也能理解并守纪。
> 权威条款源：`docs/IRON_RULES.md`；项目索引：`D:\Architecture\.workbuddy\memory\MEMORY.md`。
> 配套：`环境复现指南.md`（怎么跑）、`docs/dataflow-diagram.mermaid`（系统长什么样）。

每条结构：**铁律 → 背景（为什么定） → 违反代价（实证） → 来源**。

---

## A. 数据真相与口径

### IR-01 盘口线 SSoT（唯一真相源）
- **背景**：假 0-0 污染暴露后，发现回测若直读 `match_outcomes` 盘口列会被脏盘口带偏；故定 `opening_line.build_opening_lines()` 为盘口线唯一真相源，OU/AH 回测一律走 `clean_outcomes` + `build_opening_lines`，禁直读盘口列。
- **违反代价**：OU/AH 回测曾因错盘口线产生伪造 edge（污染 OU-over −4.34pp）。
- **来源**：2026-09-01 数据治理。

### IR-02 CS 格式铁律
- **背景**：比分须英文冒号 `0:0`，仅赛前采集，赔率域 (0,1000]。模型输出冒号 `1:1` 与真实横杠 `1-2` 格式不一曾导致匹配失败。
- **违反代价**：CS 推荐与真赛果对不上（08-31 实盘踩坑，格式坑）。
- **来源**：08-31 实盘。

### IR-03 CS=诱导器，OU=诚实锚
- **背景**：CS（波胆）最便宜波胆命中仅 9.4% 且为庄家资金引导器；OU 才是诚实锚。分析时 CS 只作"庄家在引导什么"的信号，不 obligation 跟。
- **违反代价**：误信 CS 命中率会反向操作（被诱导）。
- **来源**：CS 波胆研究。

### IR-04 分析三件套（win_rate/implied/edge_pp）
- **背景**：+EV 唯一判据要求报 ROI 必带胜率/隐含/edge_pp 三件套，单报 ROI 会被误读。
- **违反代价**：把噪声 ROI 当真 edge 采纳。
- **来源**：+EV 口径铁律。

### IR-16 队名仅 mhn/man
- **背景**：队名只用 `match_home_name`/`match_away_name`（中文 mhn）与 `man`（英文映射）；禁混用别名导致配对失败。`team_canonical` 桥接仅 3.9% 覆盖 GQ，已弃用中↔英桥。
- **违反代价**：配对错位、特征查不到。
- **来源**：team_canonical 桥接失效。

### IR-17 EV/edge 同口径
- **背景**：报 ROI 必带 win_rate/implied/edge_pp；**禁**把"抽水吃掉偏态→无 edge"当作终结论（那是被误导的终止，掩盖了真 edge 检验）。
- **违反代价**：用错误口径假称"无 edge"或假称"有 edge"。
- **来源**：+EV 口径铁律。

### IR-18 单庄 + margin>15% 价值层打折
- **背景**：单庄且抽水 >15% 时，价值层信号须打折——庄家定价更不可信。
- **违反代价**：高估 edge。
- **来源**：价值层校准。

### IR-19 低级别贝叶斯正则化（min6/max14）
- **背景**：低级别联赛数据稀疏，贝叶斯估计须正则化（min6/max14）防把噪声当信号。
- **违反代价**：把采样噪声当真实倾向（如某低级别队"主场龙"实为小样本幻觉）。
- **来源**：低级别覆盖实证。

### IR-25 obscure 领先后收缩不可靠
- **背景**：低级别联赛"领先后收缩防守"假设不可靠——体能/战术纪律差，领先方未必守得住。
- **违反代价**：判"不进球/不追平"错（Kabuscorp 0-1→终场 2-1 主胜逆转；特尔纳瓦 76' 1-2→终场 2-2 追平）。
- **来源**：两次实战证伪（2026-07）。

---

## B. 诚实与产品定位（核心隐喻区）

### IR-20 分析非预测
- **背景**：系统定位 = **「辅助诊断透镜」非「开处方」**。模型价值在"把现有赔率报告读对"，不是"造报告"或当下注建议（核心隐喻：模型=医生，赔率=检查报告，比分=病名）。
- **违反代价**：误导用户把分析当预测/下注依据。
- **来源**：核心隐喻（用户 09-02）。

### IR-21 建仓人工审批（禁实盘）
- **背景**：任何建仓须人工审批，**全程禁实盘**（纸盘/回测除外）。
- **违反代价**：自动下注 = 违规 + 真实亏损（P0 已证无 beat-market edge，自动下注必亏）。
- **来源**：IR-30 派生 + 估值纪律。

### IR-30 宁 PASS 不伪造
- **背景**：诚实医生底线"绝不伪造读片结果"。**估值拒绝收益法**（无 edge 给收益估值=伪造 edge，违 IR-30）；P0 失败仍诚实判 **FAILED** 而非粉饰。
- **违反代价**：粉饰 = 对涛哥/市场撒谎 + 自毁系统可信度（比"无 edge"更致命）。
- **来源**：核心隐喻（用户 09-02）。

### IR-31 生成的 .md 进知识库
- **背景**：生成的 .md 须跑 `build_kb.py` 进知识库（`.workbuddy/memory` 为独立体系，不混）。
- **违反代价**：知识孤岛、结论不可检索复用。
- **来源**：知识库铁律。

---

## C. 运行环境与工程纪律

### IR-09 venv shim + worker 整树
- **背景**：裸 python 3.13 / sklearn 1.9 与生产 venv 3.12 / sklearn 1.6 不一致（`InconsistentVersionWarning`）；worker 须整树用 `.venv`。
- **违反代价**：模型反序列化/行为不一致、隐性数值漂移。
- **来源**：运行环境铁律。

### IR-10 复训后 restart_bridge 手术刀重启
- **背景**：复训产出新模型须重启 bridge，但只重启服务不杀 worker 整树会残留旧模型在内存。
- **违反代价**：旧模型仍在服务（新模型未生效）。
- **来源**：模型冻结机制配套。

### IR-11 日志 ASCII
- **背景**：中文日志在非 UTF8 终端/采集器乱码，诊断困难。
- **违反代价**：日志不可读、故障难定位。
- **来源**：日志规范。

### IR-12 setError 须 string
- **背景**：`ErrorEnvelope.message` 须 string（IR 中间件 `to_str`）；抛非 string 对象会被前端当异常渲染白屏。
- **违反代价**：前端白屏。
- **来源**：API 错误处理（sequence-diagram ②）。

### IR-13 前端用 server_now
- **背景**：前端时间须用服务端时钟，避免客户端时区/时钟漂移导致分钟级对齐失败。
- **违反代价**：滚球分钟对齐错、λ 口径错。
- **来源**：滚球修正。

### IR-14 GQ busy_timeout=30000
- **背景**：采集高并发写 `events.db` 易 `database is locked`；设 30s 忙等待。
- **违反代价**：`database is locked` 刷屏、丢写（采集器重启死循环曾与此相关）。
- **来源**：采集稳定性。

### IR-15 改前 grep / py_compile + 回归
- **背景**：改生产代码须先 grep 影响面 + `py_compile` + 回归，防改坏。
- **违反代价**：隐形回归（如前端删 356 行死数据前的校验缺口）。
- **来源**：工程纪律。

### IR-27 密钥 XXX
- **背景**：密钥/API key/token 一律 `XXX` 替换，不入库、不明文外传。
- **违反代价**：密钥泄露。
- **来源**：密钥规范。

### IR-28 IPv6 坏，强制 IPv4
- **背景**：本机 DNS 返 IPv6 但路由不通，git/curl 连 github 21s 超时；`git config --global http.curloptResolve "github.com:443:20.205.243.166"` 钉死 IPv4。
- **违反代价**：拉代码/依赖卡死。
- **来源**：2026-07-13 网络坑。

---

## D. 赔率分析框架

### IR-22 陷阱在顺人线 / 低水=聪明边
- **背景**：庄家最顺人性的盘口线往往是陷阱；单张 live 低水 = 庄家在护的"聪明边"。曾把低水说成"诱多陷阱"被打脸（特尔纳瓦 2-2）。
- **违反代价**：反向误判（把庄家真实预期当陷阱）。
- **来源**：特尔纳瓦 2-2 打脸（2026-07-10）。

### IR-23 单张 live 低水跟随（不升级为陷阱）
- **背景**：无开盘价时，低水线只标注"庄家倾向"跟随，**不升级为陷阱**；真陷阱判定必须有开盘→收盘漂移（drift）证据。
- **违反代价**：无漂移数据却判陷阱 = 伪造判定。
- **来源**：低水规则修正（2026-07-10）。

### IR-24 直播比分=真相
- **背景**：直播中用户的实时比分是地面真相；web 查证（flashscore 等）只作辅证，绝不覆盖用户实时反馈。
- **违反代价**：用 web stale 终场闭环错（阿什杜德用户 80' 报 1-2 却用 web "0-2 终场"；Kabuscorp web "0-2"→实际 2-1）。
- **来源**：两次 web 误判（2026-07-10）。

### IR-26 三段框架
- **背景**：分析统一三段框架（市场锚 → 模型矩阵 → 一致性 + Edge 三件套）。
- **违反代价**：各模块口径不一、结论不可比。
- **来源**：分析框架规范。

---

## E. 滚球 / in-play

### IR-06 状态单调
- **背景**：采集状态机须单调推进，禁回退，避免重复/错态。
- **违反代价**：数据乱序、重复写入。
- **来源**：采集状态机规范。

### IR-07 in-play λ 贝叶斯
- **背景**：滚球 λ 用贝叶斯随证据更新，非固定常量（曾出现死变量导致 λ 虚高）。
- **违反代价**：λ 虚高 12%（sport_key 空串误触发 goal_scale 1.35）。
- **来源**：滚球修正（6 层修复）。

### IR-08 逆转以当前比分为先验（<10%）
- **背景**：逆转概率以当前比分为先验，不凭空假设；落后方逆转真实 <10%。
- **违反代价**：高估逆转、误导破蛋信号。
- **来源**：in-play 贝叶斯。

---

## F. 采集与闭环

### IR-05 GQ token
- **背景**：`GQ_REQUEST_ID` = 乐鱼 URL 后十六进制前 18 位 CUID；断采用 `matches.last_seen`。
- **违反代价**：换号/断流未发现（2026-09-16 19:5x 换号踩坑）。
- **来源**：乐鱼采集。

### IR-29 赔率截图入库闭环
- **背景**：用户发赔率截图须自动 OCR → 写 `odds_db/{match}.json` → 更新 `index.json` → 更新日志，静默完成（用户四段画像铁律）。
- **违反代价**：截图堆积未结构化、后续无法检索。
- **来源**：截图入库铁律（2026-07-03）。

---

## 元背景：为什么这一整组铁律存在
- **诚实医生隐喻**（用户 09-02）：模型=医生，赔率=检查报告，比分=病名。医生底线=绝不伪造读片结果 → 直接派生 IR-20/21/30。
- **数据地雷倒逼口径**：假 0-0（62%）、events.db 平局膨胀（35-42%）、GQ ht_score 66% 回填 → 倒逼 IR-01/04/25 等真相口径。
- **P0 诚实弧**：events.db 污染曾让 P0 假判"失败"；重建 GQ 干净真相（2540 场）后重测，independent_model 同代重训仍无 edge → P0 诚实判 **FAILED**。这条弧是 IR-20/21/30 最硬的实证支撑：**宁 PAS S 不伪造，能力未验证就不给收益估值**。
- **工程坑倒逼纪律**：IPv6（IR-28）、venv 版本错（IR-09）、database is locked（IR-14）、前端白屏（IR-12）等实战故障 → 各自铁律。
